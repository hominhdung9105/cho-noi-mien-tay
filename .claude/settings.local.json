{
  "permissions": {
    "allow": [
      "Bash(tail -25 /tmp/chonoi-dev1-test.log 2>/dev/null || echo \"log not created yet\")",
      "Read(//tmp/**)",
      "Bash(grep -iE \"_Project.*error CS|Assets[\\\\\\\\/]_Project\" /tmp/chonoi-dev1-test.log)",
      "Bash(grep -iA3 \"com.unity.shadergraph\" Packages/packages-lock.json)",
      "PowerShell($procs = Get-Process -ErrorAction SilentlyContinue | Where-Object { $_.ProcessName -match 'Unity' } | Select-Object ProcessName, Id; if \\($procs\\) { \"RUNNING:\"; $procs | Format-Table -AutoSize | Out-String } else { \"NO UNITY PROCESS RUNNING\" })"
    ]
  }
}
